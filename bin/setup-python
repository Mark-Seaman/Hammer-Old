#!/bin/bash
# Setup the Python Evironment

# Create a Python virtualenv for Python 2.7
virtualenv --no-site-packages --distribute -p python2.7 env

# Activate the virutalenv
. env/bin/activate


# Install Django and support libraries
pip install --upgrade pip
pip install celery redis nose
pip install cookiecutter
pip install django-floppyforms
pip install Django==1.7.3 
pip install docopt
pip install mkdocs
pip install pandoc
pip install requests
pip install tox


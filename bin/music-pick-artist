#!/bin/bash
# Select an artist to copy to the Music folder

# Remove the old artist
[[ -d ~/Music/Artist ]] && rm -rf  ~/Music/Artist

# Select a new one
artist=`ls $l/Music/Rock|random`
echo "Copy $artist"

# Copy the folder
echo cptree \'"$l/Music/Rock/$artist"\' "$HOME/Music/Artist" | sh

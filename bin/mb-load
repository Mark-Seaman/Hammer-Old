#!/bin/bash
# Load data into the MyBook database

echo Loading the MyBook database

# Create the database tables to match the models
manage.py syncdb

# Load the data from a text file
manage.py loaddata data/all.json

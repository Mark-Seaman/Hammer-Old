#!/usr/bin/env bash
# rename files that end in $1 to be $2

# usage:
#   rename-files bad good

# Alternate
# for f in *; do mv "$f" "${f/OLD_STUFF/NEW_STUFF}"; done

for FN in *.$1
do
     mv "${FN}" "${FN/.$1/.$2}"
done



#!/bin/bash
# Publish the directory in the work flow manager

dir=$1
# Select the unique files in the requested directory

common-1-only $y/$dir $y/SeamansLog |
while read f
do
    echo work-flow MyBook/$dir/$f publish
done

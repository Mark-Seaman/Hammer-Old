#!/bin/bash
# Create a new brain topic
echo Create a new log topic for $1

if [ -f "$y/SeamansLog/$1" ]
then
    echo File already exists
else
    sed "s/\* \*/*$1 */" $y/NewTopic | sed 's/\([A-Z]\)/ \1/g' > $y/SeamansLog/$1
fi
e $y/SeamansLog/$1

#!/bin/bash
# Format the text for a book
echo Format the text for a book

[[ ! -f $y/$1/Index ]] && echo "Could not find $y/$1/Index" && exit 1

wiki-links $y/$1/Index | while read f
do
    cat $y/$1/$f | wiki-text 
done
#!/usr/bin/python
# Suggest an activity

from sys import stdin

for f in open('/home/seaman/Documents/Brain/Activities').readlines():
     if f.find('            ')==0: 
         print  category[:-1]+':',f[12:-1]
     else:
         if f.find('        ')==0: 
             category = f[8:]

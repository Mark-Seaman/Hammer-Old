#!/usr/bin/python
# Calculate the invoice amount from the time summary

from sys          import stdin,argv
from util.invoice import print_time_sheet


rate = 75
if len(argv)>1:
    rate = int(argv[1])

time_text = stdin.read().split('\n')
print_time_sheet(time_text, rate)

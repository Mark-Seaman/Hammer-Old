#!/bin/bash
# Create the directory and files for this new Django application.

project=$1
appName=$2
className=$3

django-admin.py startproject $project

# Create the app 
cd $project
manage.py startapp $appName

#!/bin/bash
# Calculate the difference between two lists of directories

cd $1
find .  -maxdepth 4 -type d | sort > /tmp/diff1

cd -
cd $2
find .  -maxdepth 4 -type d | sort > /tmp/diff2

diff /tmp/diff1 /tmp/diff2

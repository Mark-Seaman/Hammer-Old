#!/bin/bash
# Publish the content for the mybook library onto the server

# Commit all outstanding changes
commit

# Sync the files and restart apache at web faction
echo  'wf-deploy' | ssh shrinking-world.org 2>&1 | mklog mb-deploy

# Run firefox
rbg firefox http://shrinking-world.org

 

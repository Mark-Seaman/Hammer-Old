#!/bin/bash
# Test the Impage SRS code

. ~/Projects/impact/bin/project > /dev/null

git pull &&
tresults | mklog tst-impact


log=$HOME/Documents/logs/`hostname`/tst-impact.log
cat $log | range 0 0
echo $log
cat $log

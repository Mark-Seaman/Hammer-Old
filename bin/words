#!/bin/bash
# Count the words on stdin

cat | tee /tmp/words

echo
echo '------------------------'
echo `cat /tmp/words|wc -w` Words 
echo `cat /tmp/words|wc -l` Lines
echo '------------------------'

rm /tmp/words


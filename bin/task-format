#!/usr/bin/env python
# Format a daily schedule

from sys import stdin
text = stdin.read().split('\n')
for line in text:
    if len(line)>0 and line[0].isdigit():
        print ' '+line[9:]
    else:
        print line.replace('**','')

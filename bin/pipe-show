#!/bin/bash
# Show the pipe scan home page
echo 'Show the pipe scan home page in a browser'

# Kill the old process
killall node

# Start the server
cd ~/Documents/Code/hammer/pipescan
rbg node app.js

# Show the web page
w localhost:8080
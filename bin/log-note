#!/bin/bash
# Send log files to evernote

logs=$HOME/Documents/logs/`hostname`
cat $logs/tst-home.log > /tmp/logs

x=`cat /tmp/logs|lc`
max=10
[ $x -gt $max ] && 
echo "Log File has too many lines $x" >> /tmp/logs  &&
#evernote /tmp/logs &&
echo "Log File has too many lines $x"

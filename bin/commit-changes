#!/bin/bash
# Find all changes
  
# Go to top of screen
reset

cd ~/Documents &&
commit 'Auto commit Documents' | git-filter

cd ~/Books &&
commit 'Auto commit Books'     | git-filter

#[ -d ~/Projects/thumper ] &&
#cd ~/Projects/thumper &&
#commit 'Auto commit App Thumper'     | git-filter

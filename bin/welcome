#!/bin/bash
# Welcome banner

[ `hostname` == seaman-macbook.local ]  && echo macbook|figlet && exit
[ `hostname` == seaman-mini.local ]  && echo macmini|figlet && exit

hostname | sed 's/\-/\n/' | figlet

